# Data artifacts live on the volume, never in the image.
raw/
*.db
index/
data/
reports/
notes/
# Environments and tooling.
.venv/
.git/
.github/
.pytest_cache/
__pycache__/
tests/
scripts/
docs/
.env
.env.*
# The web app is built inside the image (node stage); a laptop build must not leak in.
web/node_modules/
web/dist/
